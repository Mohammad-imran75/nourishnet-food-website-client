
import { useLoaderData } from "react-router-dom";

const Modal = () => {
    
  const requesFood = useLoaderData();
  console.log(requesFood);
  return (
    
    
   <div>theowidskafl</div>

   
  );
};

export default Modal;
